name: gorec

x-logging: &default-logging
  driver: json-file
  options:
    max-size: 10m
    max-file: "3"

x-labels: &managed-labels
  dev.reibik.gorec.managed: "true"

services:
  postgres:
    image: postgres:15-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_INITDB_ARGS: --encoding=UTF8 --locale=C
      TZ: ${TZ:-Europe/Moscow}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - backend
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    logging: *default-logging
    labels: *managed-labels

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes", "--maxmemory", "256mb", "--maxmemory-policy", "allkeys-lru"]
    volumes:
      - redis_data:/data
    networks:
      - backend
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
    logging: *default-logging
    labels: *managed-labels

  bot:
    build:
      context: ${BOT_SOURCE_DIR}
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - ${BOT_ENV}
    environment:
      DOCKER_ENV: "true"
      DATABASE_MODE: auto
      POSTGRES_HOST: postgres
      POSTGRES_PORT: "5432"
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      REDIS_URL: redis://redis:6379/0
      TZ: ${TZ:-Europe/Moscow}
      LOCALES_PATH: /app/locales
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ${DATA_ROOT}/bot/logs:/app/logs:rw
      - ${DATA_ROOT}/bot/data:/app/data:rw
      - ${DATA_ROOT}/bot/uploads:/app/uploads:rw
      - ${DATA_ROOT}/bot/locales:/app/locales:rw
      - ${DATA_ROOT}/bot/vpn_logo.png:/app/vpn_logo.png:ro
    networks:
      - backend
      - edge
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import os,urllib.request; r=urllib.request.Request('http://127.0.0.1:8080/health',headers={'X-API-Key':os.getenv('WEB_API_DEFAULT_TOKEN','')}); urllib.request.urlopen(r,timeout=5)\""]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    logging: *default-logging
    labels: *managed-labels

  cabinet:
    build:
      context: ${CABINET_SOURCE_DIR}
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-/api}
        VITE_TELEGRAM_BOT_USERNAME: ${VITE_TELEGRAM_BOT_USERNAME}
        VITE_APP_NAME: ${VITE_APP_NAME:-VPN Cabinet}
        VITE_APP_LOGO: ${VITE_APP_LOGO:-V}
    restart: unless-stopped
    networks:
      - edge
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:80/"]
      interval: 20s
      timeout: 5s
      retries: 5
      start_period: 15s
    logging: *default-logging
    labels: *managed-labels

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    depends_on:
      bot:
        condition: service_healthy
      cabinet:
        condition: service_healthy
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ${CONFIG_ROOT}/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - edge
    healthcheck:
      test: ["CMD", "caddy", "validate", "--config", "/etc/caddy/Caddyfile"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    logging: *default-logging
    labels: *managed-labels

  xray-statuspage:
    profiles: [xray-monitoring]
    image: gorec/xray-checker-statuspage:local
    build:
      context: ${XRAY_STATUS_SOURCE_DIR}
      dockerfile: Dockerfile
      args:
        VERSION: ${XRAY_STATUS_VERSION:-go-build}
    restart: unless-stopped
    environment:
      BOT_TOKEN: "${XRAY_STATUS_BOT_TOKEN:-}"
      BOT_ADMIN_IDS: "${XRAY_STATUS_ADMIN_IDS:-}"
      CHECKER_URL: http://127.0.0.1:2112
      SUBSCRIPTION_URL: "${XRAY_SUBSCRIPTION_URL:-}"
      TZ: "${TZ:-Europe/Moscow}"
      TITLE: "${XRAY_STATUS_TITLE:-Статус серверов}"
      DOMAIN: "${XRAY_STATUS_DOMAIN:-}"
      PORT: "8080"
      INTERNAL_PORT: "8081"
      TLS_MODE: "off"
    volumes:
      - ${DATA_ROOT}/xray-statuspage:/data
    networks:
      - edge
    logging: *default-logging
    labels: *managed-labels

  xray-checker:
    profiles: [xray-monitoring]
    image: "${XRAY_CHECKER_IMAGE:-kutovoys/xray-checker:latest}"
    restart: unless-stopped
    network_mode: service:xray-statuspage
    depends_on:
      xray-statuspage:
        condition: service_started
    environment:
      SUBSCRIPTION_URL: http://127.0.0.1:8081/sub
      SUBSCRIPTION_UPDATE: "true"
      SUBSCRIPTION_UPDATE_INTERVAL: "${XRAY_CHECK_INTERVAL:-300}"
      SUBSCRIPTION_JSON_FORMAT: "true"
      PROXY_CHECK_INTERVAL: "${XRAY_CHECK_INTERVAL:-300}"
      METRICS_HOST: 0.0.0.0
      METRICS_PORT: "2112"
      METRICS_PROTECTED: "false"
      WEB_PUBLIC: "false"
      TZ: "${TZ:-Europe/Moscow}"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:2112/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    logging: *default-logging
    labels: *managed-labels

networks:
  backend:
    internal: true
    labels: *managed-labels
  edge:
    labels: *managed-labels

volumes:
  postgres_data:
    labels: *managed-labels
  redis_data:
    labels: *managed-labels
  caddy_data:
    labels: *managed-labels
  caddy_config:
    labels: *managed-labels
